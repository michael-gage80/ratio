{
  "name": "ratio-functions",
  "private": true,
  "main": "lib/index.js",
  "engines": {
    "node": "22"
  },
  "scripts": {
    "build": "tsc && cp ../ratio/Resources/Content/diagnostic-bank.json lib/ && rm -rf lib/lessons && cp -R ../ratio/Resources/Content/lessons lib/lessons",
    "lint": "tsc --noEmit",
    "test": "npm run build && node --test 'lib/**/*.test.js'"
  },
  "dependencies": {
    "@google-cloud/vision": "^6.1.0",
    "fast-xml-parser": "^5.11.1",
    "firebase-admin": "^14.4.0",
    "firebase-functions": "^7.4.0"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "typescript": "^7.0.2"
  }
}
